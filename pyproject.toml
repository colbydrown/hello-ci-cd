[project]
name = "hello-ci-cd"
version = "0.1.0"
description = "A small project for CI/CD testing"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pytest"]

[tool.uv]
python-preference = "only-managed"
